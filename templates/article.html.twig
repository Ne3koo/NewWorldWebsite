{% extends "base.html.twig" %}

{% block content %}

    <main>

        <h2>{{ article.title }}</h2>

        <p>{{ article.content }}</p>

        <img src="{{ asset('assets/image') ~ article.image }}" alt="">

        <h3>{{ article.title2 }}</h3>

        <p>{{ article.content2 }}</p>

        <p>{{ article.content3 }}</p>

        <p>{{ article.date|date("m/d/y") }}</p>

        <button><a href="{{ path('accueil') }}">Retour à l'accueil</a></button>

    </main>

{% endblock %}